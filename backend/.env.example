# Database Configuration
# 本番環境では必ず環境変数 DATABASE_CONNECTION_STRING を設定してください
DATABASE_CONNECTION_STRING=Host=localhost;Port=5432;Database=university_management;Username=postgres;Password=your_secure_password

# 使用例:
# 開発環境:
#   export DATABASE_CONNECTION_STRING="Host=localhost;Port=5432;Database=university_management;Username=postgres;Password=dev_password"
#
# 本番環境（Docker）:
#   docker run -e DATABASE_CONNECTION_STRING="Host=db;Port=5432;Database=university_management;Username=app_user;Password=${DB_PASSWORD}" ...
#
# 本番環境（Kubernetes）:
#   secretから環境変数として注入
